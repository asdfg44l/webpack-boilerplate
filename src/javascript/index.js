// const Handlebars = require('handlebars')

// //data (json or js)
// import testJSON from '../assets/data/test.json'

// //template
// const { default: testTemplate } = require('../partials/test.html')

// //combine data and template before load
// const renderTemplate = (data, template, anchor) => {
//     const content = Handlebars.compile("{{testStr}}")(data)
//     $(anchor).html(content)
//     console.log(content)
// }

// renderTemplate(testJSON, testTemplate, "#test")
